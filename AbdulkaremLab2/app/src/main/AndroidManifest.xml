<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abdulkarem.Alani.S300993768">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AbdulkaremLab2">
        <activity android:name=".AbdulkaremPayment"></activity>
        <activity android:name=".AbdulkaremCheckoutScreen" />
        <activity android:name=".AbdulkaremHomeType" />
        <activity android:name=".AbdulkaremMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>